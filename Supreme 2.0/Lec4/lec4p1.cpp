#include <iostream>
using namespace std;
int main()
{
    int n;
    if(cin>>n)
    cout << "Hello Mitronn!! "<< endl;
    n=10;
    //cout.setstate(ios::failbit); // Manually set cout to a failed state
    if(cout<<n)
    cout<<"Hello Mitronn Dobara!!";
    return 0;
}
